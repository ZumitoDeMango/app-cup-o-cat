// import 'package:flutter/material.dart';

// class FavoritesManager {
//   // Lista estática para almacenar productos favoritos.
//   static final List<Map<String, dynamic>> favoriteProducts = [];

//   // Método para agregar un producto a la lista de favoritos.
//   static void addFavorite(Map<String, dynamic> product) {
//     favoriteProducts.add(product);
//   }

//   // Método para eliminar un producto de la lista de favoritos.
//   static void removeFavorite(Map<String, dynamic> product) {
//     favoriteProducts.removeWhere((item) => item['id'] == product['id']);
//   }

//   // Método para verificar si un producto ya está en favoritos.
//   static bool isFavorite(Map<String, dynamic> product) {
//     return favoriteProducts.any((item) => item['id'] == product['id']);
//   }
// }
